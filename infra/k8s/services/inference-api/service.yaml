apiVersion: v1
kind: Service
metadata:
  name: inference-api
  namespace: ai-personalization-platform
spec:
  selector:
    app: inference-api
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
